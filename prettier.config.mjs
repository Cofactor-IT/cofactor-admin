/**
 * prettier.config.mjs
 *
 * Shared code formatting rules for the Admin repository.
 */

const prettierConfig = {
    tabWidth: 4,
    useTabs: false,
    singleQuote: true,
    semi: true,
    trailingComma: 'es5',
    printWidth: 100,
    jsxSingleQuote: false,
    bracketSpacing: true,
    arrowParens: 'always',
    endOfLine: 'lf',
};

export default prettierConfig;
