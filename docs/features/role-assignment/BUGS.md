# Known Issues and Fixes (CA-10)

## Current Status

No known bugs specific to role-assignment enforcement.

## Notes

- Next.js warns that `middleware.ts` is deprecated in favor of `proxy.ts`.
  - This does not break current behavior.
  - Migration to `proxy.ts` should be planned in a future platform task.
