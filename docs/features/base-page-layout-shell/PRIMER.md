# Base Page Layout Shell (CA-35)

## Purpose

Provide a single shared page shell for Admin so every authenticated page uses the same sidebar, header, and content spacing.

## Scope

- Fixed desktop sidebar
- Shared page header with title and actions slot
- Shared content padding
- Global dark Admin background

## Intent

Future Admin pages should extend this shell instead of making per-page layout choices.
